# .env.example
# Environment variables for EmailJS (used by the contact form)
# ------------------------------------------------------------
# 1) Copy this file to a new file named ".env" in the project root.
# 2) Fill the values below with the credentials you get from EmailJS.
# 3) NEVER commit your real .env to version control (it's included in .gitignore).
#
# Example: VITE_SERVICE_ID=service_xxx123
# Example: VITE_TEMPLATE_ID=template_abcd456
# Example: VITE_PUBLIC_KEY=user_ABCdef123

VITE_SERVICE_ID=your_emailjs_service_id
VITE_TEMPLATE_ID=your_emailjs_template_id
VITE_PUBLIC_KEY=your_emailjs_public_key
